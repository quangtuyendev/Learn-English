import React, { Fragment } from "react";
import WordsListContainer from '../WordsList/WordList';

export const Home = () => {
    return (
        <Fragment>
            <WordsListContainer />
        </Fragment>
    );
};